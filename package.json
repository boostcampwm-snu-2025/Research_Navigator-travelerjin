{
  "name": "smartmagazine-travelerjin",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "concurrently -n frontend,backend -c \"auto\" \"npm run dev --prefix frontend\" \"npm run dev --prefix backend\"",
    "build": "npm run build --prefix frontend && npm run build --prefix backend",
    "start": "npm run start --prefix backend"
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  }
}
